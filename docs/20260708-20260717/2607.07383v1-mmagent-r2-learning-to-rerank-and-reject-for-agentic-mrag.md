---
title: "MMAgent-R$^2$: Learning to Rerank and Reject for Agentic mRAG"
title_zh: MMAgent-R$^2$：学习重排序与拒绝以实现智能体式多模态检索增强生成
authors: "Tao Zhang, Ziqi Zhang, Zongyang Ma, Yuxin Yang, Bing Li, Chunfeng Yuan, Kang Rong, Fengyun Rao, Jing Lyu, Weiming Hu"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07383v1"
tags: ["query:agentic-rag"]
score: 9.0
evidence: 学习重排序与拒绝的agentic多模态RAG，以排除混淆实体
tldr: 在基于知识库的视觉问答中，传统mRAG方法依赖全局视觉特征，难以区分视觉相似但事实无关的实体，导致候选集污染和错误传播。为此，提出MMAgent-R^2框架，创新性地集成视觉重排序与主动拒绝作为内部验证机制，视觉重排序通过直接比对图像捕捉判别细节，主动拒绝在置信度低时动态扩充候选。采用复合奖励与GRPO联合优化检索、验证和生成，在InfoSeek、E-VQA和MMhops上取得最优性能，尤其在复杂检索与多图多跳推理中优势显著，为构建可信赖的代理式mRAG系统提供新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07383v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 858, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1266, \"height\": 730, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 760, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1257, \"height\": 649, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07383v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 844, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 611, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 644, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 666, \"height\": 179, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 512, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 583, \"height\": 210, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 538, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 956, \"height\": 344, \"label\": \"Table\"}]"
motivation: 现有mRAG方法依赖全局视觉特征，易将视觉相似但事实无关的实体误判为相关，导致检索错误无法纠正并传播至下游。
method: 提出MMAgent-R^2框架，通过视觉重排序直接比对查询与候选图像，同时利用主动拒绝在低置信度时舍弃不可靠结果并检索更多候选，使用GRPO联合训练检索、验证与生成。
result: 在InfoSeek、E-VQA和MMhops数据集上达到领先性能，在挑战性检索和多图像多跳推理任务中优势尤为突出。
conclusion: 验证了内部验证机制对提升mRAG鲁棒性的关键作用，为自验证的代理式知识增强生成树立了新范式。
---

## 摘要
基于知识的视觉问答要求模型从大规模百科知识库中检索与查询图像匹配的视觉实体并回答相关问题。现有多模态检索增强生成方法依赖全局视觉特征匹配候选实体，但当知识库包含大量视觉相似的实体时，检索器难以区分它们，导致候选集中出现视觉相似但事实不匹配的干扰项。由于后续处理步骤（如噪声过滤）也局限于这一固定候选集，检索失败的错误不可避免地传播到最终答案。为应对这些挑战，我们提出MMAgent-R$^2$，一个将视觉重排序与主动拒绝作为内部验证机制整合的智能体式多模态检索增强生成框架。视觉重排序直接比较查询图像与候选图像，捕捉文本描述无法体现的判别细节，从而在相似候选中精确识别目标实体；主动拒绝则在未找到确信匹配时丢弃不可靠结果并检索额外候选，突破固定候选池的限制。我们设计了一个包含步骤级验证奖励的复合奖励函数，并通过GRPO训练实现外部检索、内部验证与答案生成的联合优化。在InfoSeek、E-VQA和MMhops上的实验表明，我们的方法取得了最先进的性能，在具有挑战性的检索场景和复杂的多图像多跳推理任务中优势尤为显著。

## Abstract
Knowledge-based Visual Question Answering (KB-VQA) requires models to retrieve visual entities matching the query image from large-scale encyclopedic knowledge bases and answer related questions. Existing multimodal Retrieval Augmented Generation (mRAG) methods rely on global visual features to match candidate entities, yet when the knowledge base contains numerous visually similar entities, the retriever struggles to distinguish them, populating the candidate set with visually similar but factually mismatched distractors. Since subsequent processing steps such as noise filtering are also confined to this fixed candidate set, errors from failed retrieval inevitably propagate to the final answer. To address these challenges, we propose MMAgent-R$^2$, an agentic mRAG framework that integrates visual reranking and active rejection as its internal verification mechanism. Visual reranking directly compares query and candidate images, capturing discriminative details beyond textual descriptions to precisely identify the target entity among similar candidates; active rejection discards unreliable results and retrieves additional candidates when no confident match is found, moving beyond the fixed candidate pool. We design a composite reward function with step-level verification rewards and achieve joint optimization of external retrieval, internal verification, and answer generation via GRPO training. Experiments on InfoSeek, E-VQA, and MMhops demonstrate that \ours{} achieves state-of-the-art performance, with particularly notable advantages in challenging retrieval scenarios and complex multi-image multi-hop reasoning tasks.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化、深入、客观总结。

### 1. 论文的核心问题与整体含义

论文聚焦于**基于知识的视觉问答**任务。这类任务要求模型从大规模知识库中精确检索与查询图像相关的实体，并基于此回答问题。
-   **核心问题**：现有多模态检索增强生成方法存在一个**关键“识别瓶颈”**。当知识库中存在众多视觉高度相似的实体（如不同但外貌相近的物种、建筑等）时，初始检索阶段依赖全局视觉特征进行匹配，极易召回视觉相似但事实无关的“干扰项”。由于后续处理步骤被锁定在固定候选池内，早期的检索错误会不可避免地向后续的答案生成阶段传播，导致最终答案出错。
-   **研究动机**：作者指出，现有的后处理方案多停留于文本层面，无法捕捉实体间细微的视觉差异。因此，需要一种能从**视觉层面**对检索结果进行深度验证与纠错的机制，以打破固定候选池的限制。
-   **整体含义**：提出了一个名为 **MMAgent-R²** 的智能体式mRAG框架，通过为模型赋予**自我验证（视觉重排序）** 和**主动纠错（主动拒绝）** 的能力，解决检索错误传播问题，从而实现更可信赖的知识增强生成。

### 2. 论文提出的方法论

MMAgent-R²的核心思想是将推理过程建模为一个多轮智能体决策过程，通过**视觉重排序**和**主动拒绝**这两种内部验证动作，对检索结果进行精准把控。

-   **核心思想**：
    -   将VLM作为智能体，通过多轮“推理-行动-反馈”循环，自主调用外部检索工具，并对结果进行内部验证或动态扩充。
    -   使用**组相对策略优化**进行端到端强化学习训练，并设计了一个包含**步骤级验证奖励**的复合奖励函数，以提供密集的监督信号。

-   **关键技术细节**：
    -   **动作空间：**
        -   **外部检索**:
            -   `SearchImage (j)`：对指定查询图像检索一批视觉相似候选实体，支持动态扩展（连续调用返回不重叠的下一批）。
            -   `SearchText ({qi}mi=1 )`：并行执行多个文本查询，从文本知识库中检索相关段落。
        -   **内部验证**:
            -   `Rerank (ek)`：通过直接视觉对比，从当前候选批中选出与查询图像精确匹配的实体`ek`。
            -   `Reject ()`：当当前批次无匹配实体时，丢弃该批并触`SearchImage`获取下一批候选。设有最大拒绝次数`Rejmax`以平衡搜索深度与效率。
        -   **答案生成**:
            -   `Answer (y)`：基于所有累积证据，生成最终答案`y`。
    -   **复合奖励函数**：
        \[
        R = R_{\text{outcome}} + R_{\text{format}} + R_{\text{ver}}
        \]
        -   **结果奖励**：评估最终答案的正确性。
        -   **格式奖励**：确保输出严格遵循预定义的动作语法。
        -   **步骤级验证奖励**：这是关键的细粒度监督信号，对每一个`Rerank`或`Reject`决策进行奖励。正确选出匹配实体会获得1分奖励，在批次中无正确实体时选择`Reject`会获得一个小权重`δ`的奖励（如0.2），其他情况（如选错或不该拒绝时拒绝）则无奖励。

    -   **训练细节**：使用GRPO算法，训练时遮蔽所有来自环境的响应（如图像、文本），确保策略梯度更新仅作用于模型自身生成的“推理”与“动作”标记。

### 3. 实验设计

-   **数据集与场景**：
    -   **InfoSeek**：大规模KB-VQA数据集，包含约1.3M样本，评估子集分为“未见问题”和“未见实体”。
    -   **Encyclopedic-VQA (E-VQA)**：包含单跳和双跳推理问题，特点是知识库规模巨大（约200万维基页面），检索难度极高。
    -   **MMhops**：专为多模态多跳推理设计，包含单图链式推理（Bridging）和多图比较推理（Comparison）任务，所有样本均需3-4步推理。

-   **基准对比**：对比了三类主流方法：
    -   **直接回答**：如InstructBLIP、GPT-4V、Qwen系列等，不依赖外部检索。
    -   **多模态RAG**：如Wiki-LLaVA、EchoSight、ReAG等，采用不同检索和后处理策略。
    -   **智能体式mRAG**：如MMhops-R1，具备多轮工具调用能力。

-   **基础模型与实现**：基于Qwen2.5-VL-7B和Qwen3-VL-8B实现。视觉检索使用EVA-CLIP-8B和Faiss，文本检索使用E5。

### 4. 资源与算力

-   **算力使用**：论文在“Implementation Details”中提到了训练的全局批量大小（1024）、训练周期（2 epochs）和每实例的采样轨迹数（G=8），但**未明确提及**用于训练的具体GPU型号、数量及总训练时长。

### 5. 实验数量与充分性

-   **实验组数概览**：
    -   **主实验**：在3个数据集上对比了10余种SOTA方法，产生了**2张核心结果表格**。
    -   **实体识别评估**：通过**1张表格**单独量化了模型在视觉实体识别准确率上的提升。
    -   **消融实验**：围绕关键组件设计了**4组消融实验**（共4张表格），分别探究了重排序与拒绝机制的有效性、候选批次大小`K`、最大拒绝次数`Rejmax`及拒绝奖励权重`δ`的影响。
    -   **定性分析**：展示了**2张示例图**，对比有/无重排序-拒绝机制下的推理轨迹。

-   **充分性与公平性评估**：
    -   **充分性**：实验设计全面，覆盖了不同难度（E-VQA的挑战性检索）和不同推理模式（MMhops的多图多跳）的数据集。消融实验系统性地验证了核心组件及其关键超参数的作用，较为充分。
    -   **客观与公平性**：对比方法广泛，涵盖了同期最新的基线模型。训练数据使用子集而非全部，这可能影响与全量训练方法的直接比较，但作者在相同的实验框架下进行消融和主要对比，内部公平性有保证。然而，其优势部分源于采用了更强的视觉编码器和构建了更复杂的智能体流程，与简单管线对比可能存在架构上的先天优势。

### 6. 论文的主要结论与发现

1.  **视觉验证机制至关重要**：集成了视觉重排序和主动拒绝的MMAgent-R²在所有基准上均取得了最优性能，尤其在检索极具挑战的E-VQA数据集上，提升最为显著（相对第二名提升高达7.2个点），证明在视觉层面进行内部验证是解决相似实体混淆问题的关键。
2.  **组件协同增效**：消融实验表明，视觉重排序是性能提升的主要驱动力（通过对比提高精度），而主动拒绝机制能在此基础上进一步扩大候选覆盖范围（通过动态扩充），两者从不同维度互补，共同提升了实体识别准确率和最终VQA分数。
3.  **泛化至多跳推理**：该方法不仅能处理单实体检索，还能有效泛化到MMhops这类需要多图像输入和多跳推理的复杂任务中，性能显著优于闭源强模型，展现出良好的鲁棒性和泛化能力。
4.  **奖励设计的重要性**：步骤级验证奖励提供了关键的中间监督信号，使模型学会了何时选择、何时拒绝，避免了盲目猜测。

### 7. 优点

-   **创新性的机制设计**：提出了“视觉重排序+主动拒绝”这一简洁而有效的组合，将mRAG的后处理从文本层面提升到了视觉层面，直接解决了视觉实体困惑的核心矛盾。
-   **统一的智能体式框架**：将检索、验证、推理、生成统一于一个端到端训练的智能体内，摆脱了对固定管线或额外独立模块的依赖，架构优雅，扩展性强。
-   **精细的奖励工程**：设计的步骤级验证奖励为智能体的决策过程提供了密集、准确的反馈，是强化学习训练成功的关键，为同类问题的奖励设计提供了有价值的参考。
-   **严谨的实验验证**：在多个具有挑战性的基准上进行了全面而深入的实验，特别是在最具难度的E-VQA和MMhops数据集上的巨大提升，强有力地证明了方法的有效性。

### 8. 不足与局限

-   **检索上界限制**：论文明确指出，模型的性能仍然受制于初始检索器的召回上限。如果正确实体完全未被检索系统召回（`R@15`都无法覆盖），那么后续的验证机制也无能为力。
-   **计算成本**：将候选图像批量注入推理上下文，以及允许模型进行多轮拒绝和再检索，都会显著增加长度和标记处理量，带来更高的推理延迟和计算开销。
-   **通用性与鲁棒性**：实验仅基于两个Qwen系列模型和百科类知识库（Wikipedia），其结论能否推广到其他模型架构（如更大的专有模型）和更开放、噪声更大的知识库（如通用网页）尚待验证。
-   **超参数敏感性与训练效率**：拒绝机制的效力取决于奖励权重`δ`、批次大小`K`和最大拒绝次数`Rejmax`等多个超参数的精细调节，且强化学习训练本身收敛难度较大、训练成本较高。
-   **复杂语义缺失**：该方法主要聚焦于低层次、细粒度的**视觉特征比对**，对于需要更深层次常识、文化背景或抽象语义关联才能区分的相似实体（如“一幅表达悲伤的画作” vs “一幅表达绝望的画作”），可能作用有限。

（完）
