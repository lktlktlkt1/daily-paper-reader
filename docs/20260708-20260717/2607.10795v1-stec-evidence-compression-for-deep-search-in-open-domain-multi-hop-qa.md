---
title: "STEC: Evidence Compression for Deep Search in Open-domain Multi-Hop QA"
title_zh: STEC：面向开放域多跳问答中深度搜索的证据压缩
authors: "Xinkang Li, Rong Jiang, Xin Song, Ye Wang, Yue Han, Changjian Li"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10795v1"
tags: ["query:agentic-rag"]
score: 9.0
evidence: 面向多跳问答搜索代理的多轨迹证据压缩
tldr: 开放域多跳问答使用LLM搜索代理时，多条搜索轨迹带来最终答案选择挑战，原始轨迹比较面临噪声和不对齐。STEC框架通过答案级证据压缩，将轨迹按标准化答案分组并压缩为候选证据表示，再经过证据引导的验证机制选出最优答案。实验显示STEC在四个基准上全面领先，消融证实证据压缩是关键。贡献在于从原始轨迹比较转向候选证据比较，提升选择可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10795v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1118, \"height\": 411, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10795v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1266, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10795v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 603, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10795v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10795v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 754, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10795v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1273, \"height\": 613, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10795v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1274, \"height\": 186, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10795v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1274, \"height\": 478, \"label\": \"Table\"}]"
motivation: 多搜索轨迹导致最终答案选择困难，原始轨迹比较引入噪声和不对齐，忽略关键证据。
method: STEC通过答案级证据压缩将轨迹按答案分组转化为证据表示，再经证据引导验证选出答案。
result: 在四个基准上STEC整体最优，消融实验证明答案级证据压缩对最终答案选择有贡献。
conclusion: STEC将最终选择从原始轨迹比较转为候选证据比较，提升了多跳问答答案选择的可靠性。
---

## 摘要
在开放域多跳问答（QA）中，基于大语言模型（LLM）的搜索代理通过将检索与推理相结合，为知识密集型QA提供了一种有前景的方法。现有方法主要通过推理范式、检索交互和搜索策略优化来改进开放域多跳QA。然而，使用多条搜索轨迹带来了一个具有挑战性的最终答案选择问题。不同的轨迹可能支持不同的候选答案，检索到的信息可能异构、冗余、不完整或相互冲突。直接比较原始轨迹会使验证器暴露于嘈杂且未对齐的内容，而比较答案字符串则忽视了支持每个候选答案的证据，使得可靠的最终选择变得困难。为解决这一挑战，我们提出了STEC，一个用于多跳QA中最终答案选择的证据压缩框架。STEC通过两种机制从现有候选集中选择最终答案：（1）答案级证据压缩，按归一化的答案标识对轨迹进行分组，并将每个答案组转换为特定于候选答案的证据表示；（2）证据引导的答案验证，比较这些表示并从候选集中选择最终答案。该设计将最终选择从原始轨迹比较转变为候选级证据比较。我们在四个开放域多跳QA基准上对STEC进行了评估，并与代表性基线方法进行了比较。实验结果表明，STEC在所有比较方法中总体表现最佳，消融结果提供了证据，表明答案级证据压缩对最终答案选择有所贡献。

## Abstract
In open-domain multi-hop question answering (QA), LLM-based search agents offer a promising approach to knowledge-intensive QA by combining retrieval with reasoning. Existing methods mainly improve open-domain multi-hop QA through reasoning paradigms, retrieval interaction, and search strategy optimization. However, using multiple search trajectories introduces a challenging final answer selection problem. Different trajectories may support different candidates, and the retrieved information can be heterogeneous, redundant, incomplete, or conflicting. Directly comparing raw trajectories exposes the verifier to noisy and unaligned content, while comparing answer strings ignores the evidence supporting each candidate, making reliable final selection difficult. To address this challenge, we propose STEC, an evidence compression framework for final answer selection in multi-hop QA. STEC selects the final answer from the existing candidate set through two mechanisms: (1) Answer-Level Evidence Compression, which groups trajectories by normalized answer identity and converts each answer group into a candidate-specific evidence representation; and (2) Evidence-Guided Answer Verification, which compares these representations and selects the final answer from the candidate set. The design shifts final selection from raw trajectory comparison to candidate-level evidence comparison. We evaluate STEC on four open-domain multi-hop QA benchmarks against representative baselines. Experimental results show that STEC performs best overall among the compared methods, and ablation results provide evidence that answer-level evidence compression contributes to final answer selection.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- 开放域多跳问答（QA）要求从大规模开放语料中检索多源信息并进行多步推理，传统检索增强生成（RAG）只在生成前做一次性检索，无法根据推理中间状态动态更新检索目标。
- 近期的深度搜索（Deep Search）方法将检索与推理过程交织，生成多条搜索轨迹，每条轨迹包含查询、证据、中间推理和候选答案，增强了证据覆盖与推理深度。
- 但多轨迹输出引入了**最终答案选择的困难**：不同轨迹可能支持不同候选答案，轨迹内容异构、冗余、不完整甚至冲突。
  - 直接比较原始轨迹：验证器需处理大量噪声和不对齐内容。
  - 仅比较答案字符串：忽视了支撑每个候选答案的证据基础。
- 因此，如何将多轨迹搜索的结果有效组织，并基于证据可靠地选出最终答案，成为提升多跳QA性能的关键挑战。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

STEC 框架将最终答案选择定义为**以候选答案为中心的证据比较问题**，主要包括三个阶段：

#### （1）多轨迹搜索（Multi-Trajectory Search）
- 对一个问题 \( q \) 使用搜索生成器进行 \( N \) 次搜索 rollout，得到轨迹集 \( \mathcal{T}(q) = \{\tau_1, \tau_2, \dots, \tau_N\} \)，每条轨迹以原始候选答案 \( r_i \) 结尾。

#### （2）答案级证据压缩（Answer-Level Evidence Compression）
- **答案归一化**：对每个 \( r_i \) 计算归一化答案标识 \( \tilde{a}_i = \text{Norm}(r_i) \)（统一大小写、标点等）。
- **候选集构建**：\( \mathcal{A}(q) = \text{Unique}(\{\tilde{a}_i\}) \)，得到 \( M \) 个唯一候选答案。
- **答案分组**：对每个候选答案 \( a \)，构建组 \( G_a = \{\tau_i \mid \tilde{a}_i = a\} \)。
- **压缩**：将每个答案组压缩为特定候选的证据表示：
  \[
  \mathcal{C}_a = \text{Compress}(q, a, G_a) = (\mathcal{V}_a, \mathcal{E}^+_a, \mathcal{E}^-_a, \mathcal{P}_a, \Gamma_a)
  \]
  - \( \mathcal{V}_a \)：组内原始答案变体集合。
  - \( \mathcal{E}^+_a \)：支持候选 \( a \) 的证据单元。
  - \( \mathcal{E}^-_a \)：削弱或反驳 \( a \) 的证据单元。
  - \( \mathcal{P}_a \)：从支持证据到候选答案的简洁推理路径。
  - \( \Gamma_a \)：组级统计信息（如轨迹数量）。
- 压缩在每个答案组内独立进行，避免不同候选的证据混合，并合并重复证据以精简表示。

#### （3）证据引导的答案验证（Evidence-Guided Answer Verification）
- 输入为 \( \mathcal{C}(q) = \{\mathcal{C}_a \mid a \in \mathcal{A}(q)\} \)。
- 验证器比较各候选的结构化证据表示，综合评估支持证据覆盖度、推理路径连贯性、冲突证据等，从候选集中选择最终答案标识 \( \hat{a} \)，并映射回原始答案字符串。
- 选择过程限制在搜索输出的候选集内，不生成新答案。

整体流程将“粗轨迹比较”转化为“结构化证据比较”，使验证输入更聚焦、可对齐。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法

- **数据集**：HotpotQA、2WikiMultihopQA、MuSiQue、Bamboogle（覆盖多样检索与推理挑战）。
- **评估指标**：归一化后的精确匹配（Exact Match, EM），并报告四个数据集的宏平均 EM。
- **基线方法（四类）**：
  - 无检索推理：Direct Inference、Chain-of-Thought (CoT)
  - 有检索推理：RAG、IRCoT
  - 微调方法：SFT、R1-base、R1-instruct、Rejection Sampling（结合搜索引擎）
  - 搜索增强推理：Search-o1、Search-R1
- 所有方法统一使用相同检索器、知识库、文档数量和骨干模型（主实验 Qwen2.5-7B），保证对比公平。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力

论文中**未明确说明**具体的GPU型号、数量及训练/推理时长。只在实验部分提到使用特定模型（如Qwen2.5-7B）以及不同模型尺寸的泛化分析，但无详细算力消耗数据。

### 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、客观、公平

实验设计较为全面，共包含以下几组关键实验：

- **主实验**：4个数据集 × 10种方法（含STEC），报告EM及平均EM。
- **消融研究**：对比“Verifier Only”（移除证据压缩）与完整STEC，验证答案级证据压缩的必要性。
- **超参数分析（轨迹数 \( N \)）**：分别测试 \( N = 2, 4, 8 \) 下Verifier Only和STEC的性能。
- **模型泛化分析**：3B Base、3B Instruct、7B Base、7B Instruct 四种配置下比较STEC与Verifier Only。
- 所有对比均在相同设置下进行，检索条件、知识库、模型基座等保持一致，实验对比公平；通过多场景验证，结论具有一定客观性。但数据集仅限英文多跳QA，未覆盖其他语言或领域。

### 6. 论文的主要结论与发现

- STEC 在四个数据集的平均EM（0.347）上优于所有基线，分别比最强基线（Search-R1）提升0.046，比推理类、检索类、微调类最佳方法提升显著。
- 答案级证据压缩对最终答案选择有正向贡献：消融实验中完整STEC较仅验证器提升平均EM（0.339→0.347），证明结构化证据组织的重要性。
- 增加搜索轨迹数（\( N \)）通常提升性能，STEC在较大轨迹预算下仍保持优势，且答案级压缩效应随轨迹增多而更明显。
- 模型尺寸和指令微调均影响验证性能，STEC在不同模型配置下均优于无压缩方案，尤其在小模型上增益更突出，表明结构化证据能缓解模型容量限制。

### 7. 优点：方法或实验设计上有哪些亮点

- **问题视角新颖**：将多轨迹最终选择转化为候选级证据比较问题，弥补了现有方法忽略证据组织的缺陷。
- **方法设计精巧**：通过答案归一化分组、支持/冲突证据分类、推理路径提取和组级统计，构建紧凑且可比较的候选表示，解耦了证据组织与验证。
- **强实验设计**：覆盖四类代表性基线，多数据集、多模型配置、多轨迹数的消融与分析，验证证据压缩的独立贡献。
- **实用性强**：框架无需额外生成答案，仅从已有候选中选择，可与不同搜索器集成，易于部署。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **证据压缩依赖LLM能力**：压缩和验证均基于同一LLM，若模型本身推理能力弱，压缩质量可能下降，但论文未讨论低能力模型下的瓶颈。
- **实验数据集单一**：仅使用英文多跳QA数据集，缺乏其他语言、对话式QA或更复杂推理任务的验证，通用性有待延伸。
- **候选集不包含正确答案时的处理未讨论**：当搜索轨迹均未产生正确答案时，STEC无法纠正，仅从错误候选中选择，可能输出错误答案。
- **超参数敏感性**：轨迹数 \( N \) 的影响仅在2、4、8三个点考察，更细致的预算分配策略未探索。
- **公平性对比局限**：某些基线方法未专门针对多轨迹选择优化，直接对比可能低估特定调优的潜力。
- **资源消耗未知**：缺少算力开销报告，难以评估实际部署成本。

（完）
